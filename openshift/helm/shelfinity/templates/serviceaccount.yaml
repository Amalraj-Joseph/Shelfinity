{{- if .Values.security.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "shelfinity.serviceAccountName" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "shelfinity.labels" . | nindent 4 }}
{{- end }}

# Made with Bob
